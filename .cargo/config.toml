# 360 安全卫士会将个别 Rust 构建脚本路径加入永久黑名单（误报），
# 导致 cargo 无法在该目录创建/执行 build-script-build.exe。
# 下面这个无副作用的 cfg 会改变构建目录的哈希，从而绕开被封锁的旧路径。
# 如果之后在 360 中清理了相关拦截记录，可以删除本文件。
[build]
rustflags = ["--cfg", "deploycode"]
